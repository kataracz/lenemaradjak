# Trivy vulnerability waivers.
#
# Each entry is a CVE we have reviewed and consciously accept, with the reason.
# Keep this list short and revisit it when the base image is updated.
#
# Currently empty. The previous CVE-2026-12151 waiver (undici DoS) covered a
# copy vendored inside npm in the node:24-slim base image; the Dockerfile now
# removes npm from the runtime image, so the finding is gone rather than waived.
